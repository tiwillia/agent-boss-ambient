# Template for ACP_TOKEN secret
# Replace <base64-encoded-token> with: echo -n 'your-actual-token' | base64
apiVersion: v1
kind: Secret
metadata:
  name: boss-secrets
  namespace: ambient-code
type: Opaque
data:
  ACP_TOKEN: <base64-encoded-token>
